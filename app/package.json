{
  "name": "user-manager",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "jest",
    "user-manager:build": "yarn workspace user-manager build",
    "user-manager-ui:build": "yarn workspace user-manager-ui build",
    "lib:build": "yarn workspace lib build",
    "lib-ui:build": "yarn workspace lib-ui build",
    "dev-server:build": "yarn workspace dev-server build",
    "storybook:build": "yarn workspace user-manager-storybook build-storybook",
    "dev": "yarn workspace nextjs run dev & yarn workspace user-manager-storybook storybook",
    "docker-run:dev": "bash .devtools/docker-run.sh dev"
  },
  "private": true,
  "workspaces": [
    "packages/**",
    "storybook"
  ],
  "peerDependencies": {
    "jest": "^26.6.3",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/react-router-dom": "^5.1.7",
    "ts-jest": "^26.5.5",
    "ts-node": "^9.1.1"
  }
}
