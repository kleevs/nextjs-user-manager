{
  "name": "user-manager",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "jest",
    "dev": "yarn workspace nextjs run dev",
    "user-manager:build": "yarn workspace user-manager build",
    "user-manager-ui:build": "yarn workspace user-manager-ui build",
    "lib:build": "yarn workspace lib build",
    "lib-ui:build": "yarn workspace lib-ui build",
    "storybook": "yarn workspace user-manager-storybook storybook",
    "build-storybook": "yarn workspace user-manager-storybook build-storybook"
  },
  "private": true,
  "workspaces": [
    "packages/**",
    "storybook"
  ],
  "peerDependencies": {
    "jest": "^26.6.3",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/react-router-dom": "^5.1.7",
    "ts-jest": "^26.5.5",
    "ts-node": "^9.1.1"
  }
}
