{
  "name": "user-manager",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "jest",
    "dev": "yarn workspace nextjs run dev",
    "build-user-manager": "yarn workspace user-manager build",
    "build-user-manager-ui": "yarn workspace user-manager-ui build",
    "build-lib": "yarn workspace lib build",
    "build-lib-ui": "yarn workspace lib-ui build"
  },
  "private": true,
  "workspaces": [
    "packages/**",
    "server"
  ],
  "peerDependencies": {
    "jest": "^26.6.3",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/react-router-dom": "^5.1.7",
    "ts-jest": "^26.5.5",
    "ts-node": "^9.1.1"
  }
}
