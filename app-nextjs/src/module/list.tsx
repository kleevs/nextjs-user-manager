import List from 'user-manager-react/src/container/list';
import React, { useState } from 'react';

export default function ListingModule() { 
    // var [users] = useState<User[]>([]);

    return <List 
        // users={users}
    />;
}